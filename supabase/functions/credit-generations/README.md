# credit-generations

Cette fonction est **déjà déployée en production** sur `itjtshfzpknlzownpwte` (lecrin-replit, v2, RevenueCat → `user_quotas`).

Le code source historique n’est pas versionné dans ce dépôt. L’app iOS l’appelle via :

```swift
SupabaseService.shared.creditGenerations(productId:transactionId:)
```

Pour redéployer depuis le Dashboard Supabase ou récupérer le bundle depuis un backup projet.
